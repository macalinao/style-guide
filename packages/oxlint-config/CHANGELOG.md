# @macalinao/oxlint-config

## 0.2.1

### Patch Changes

- f3c01a4: Update the oxlint peer dependency to 1.73.0, matching the version provided by nixpkgs.

## 0.2.0

### Minor Changes

- 060e3aa: Add `@macalinao/oxlint-config`: an opinionated oxlint configuration for modern TypeScript projects, with a base config and an opt-in TanStack Router add-on. This is part of migrating the style guide's tooling from Biome/Prettier to oxlint (linting) and oxfmt (formatting).
