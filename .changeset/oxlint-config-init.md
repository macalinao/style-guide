---
"@macalinao/oxlint-config": minor
---

Add `@macalinao/oxlint-config`: an opinionated oxlint configuration for modern TypeScript projects, with a base config and an opt-in TanStack Router add-on.
