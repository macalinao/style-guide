---
"@macalinao/eslint-config": patch
---

Update dependencies: `eslint-config-turbo` to 2.9.18 (now sourced from the workspace catalog, pinned in lockstep with the `turbo` version provided by nixpkgs), `@typescript-eslint/parser` to ^8.64.0, `globals` to ^17.7.0, and `typescript-eslint` to ^8.64.0.
