---
"@macalinao/biome-config": minor
---

Enable the `useMaxParams` (warn) and `noUselessCatchBinding` (error) lint rules. `useMaxParams` encourages passing named arguments via objects instead of many positional parameters.
